---
title: "Configuration"
description: "Environment variables and global flags."
weight: 20
---

orv aims to need almost no configuration. As you add settings,
document each one here with its default.

## Global flags

| Flag | Meaning |
|---|---|
| `--help` | Help for any command |
| `--version` | Print the version |

## Environment variables

None yet. Add them here as the tool grows, prefixed `orv_` in upper
case (for example `orv_TOKEN`).
